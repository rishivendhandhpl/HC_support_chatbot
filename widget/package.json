{
  "name": "hc-chat-widget",
  "version": "1.0.0",
  "description": "Hair & Compounds AI Virtual Stylist — framework-free storefront chat widget",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "type-check": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vite": "^6.0.7",
    "vitest": "^2.1.8"
  }
}
